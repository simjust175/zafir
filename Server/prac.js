const names = [
    {
    name: "simcha",
    age: 29
},
    {
    name: "yanky",
    age: 29
},
    {
    name: "lemech",
    age: 29
},
    {
    name: "susha",
    age: 29
}]

// const testForDuplicateEntries = (entry, inDb)=> {
//     //const inDb = await General.get(`"${entry}"`, `${entry} = ${entry}.${entry}`)
//     return console.log(`"${inDb}"`)
// }
// console.log(testForDuplicateEntries({name: 'simcha'}, name));

const query = {
    name: "simcha"
}

const txt = "hello";
 // Output: "hell"

const wage = 120
const margin = 0
console.log(wage + (wage*(margin/100)));
console.log("name", names.includes('simcha'));
